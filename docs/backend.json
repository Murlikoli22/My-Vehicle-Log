
{
  "entities": {
    "Vehicle": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Vehicle",
      "type": "object",
      "description": "Represents a vehicle owned by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the vehicle."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who owns this vehicle. (Relationship: User 1:N Vehicle)"
        },
        "registrationNumber": {
          "type": "string",
          "description": "The vehicle's registration number."
        },
        "type": {
          "type": "string",
          "description": "The type of vehicle (e.g., car, truck, motorcycle)."
        },
        "brand": {
          "type": "string",
          "description": "The vehicle's brand (e.g., Toyota, Ford)."
        },
        "model": {
          "type": "string",
          "description": "The vehicle's model (e.g., Camry, F-150)."
        },
        "year": {
          "type": "number",
          "description": "The vehicle's manufacturing year."
        },
        "fuelType": {
          "type": "string",
          "description": "The fuel type used by the vehicle (e.g., gasoline, diesel, electric)."
        },
        "odometerReading": {
          "type": "number",
          "description": "The current odometer reading of the vehicle."
        }
      },
      "required": [
        "id",
        "userId",
        "registrationNumber",
        "type",
        "brand",
        "model",
        "year",
        "fuelType",
        "odometerReading"
      ]
    },
    "Document": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Document",
      "type": "object",
      "description": "Represents a document associated with a vehicle.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the document."
        },
        "vehicleId": {
          "type": "string",
          "description": "Reference to the Vehicle this document belongs to. (Relationship: Vehicle 1:N Document)"
        },
        "documentType": {
          "type": "string",
          "description": "The type of document (e.g., RC Book, Insurance, PUC)."
        },
        "uploadDate": {
          "type": "string",
          "description": "The date the document was uploaded.",
          "format": "date-time"
        },
        "expiryDate": {
          "type": "string",
          "description": "The expiry date of the document.",
          "format": "date-time"
        },
        "fileUrl": {
          "type": "string",
          "description": "URL of the uploaded document file.",
          "format": "uri"
        }
      },
      "required": [
        "id",
        "vehicleId",
        "documentType",
        "uploadDate",
        "expiryDate",
        "fileUrl"
      ]
    },
    "MaintenanceLog": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "MaintenanceLog",
      "type": "object",
      "description": "Represents a maintenance log entry for a vehicle.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the maintenance log entry."
        },
        "vehicleId": {
          "type": "string",
          "description": "Reference to the Vehicle this maintenance log belongs to. (Relationship: Vehicle 1:N MaintenanceLog)"
        },
        "date": {
          "type": "string",
          "description": "The date of the maintenance service.",
          "format": "date-time"
        },
        "odometerReading": {
          "type": "number",
          "description": "The odometer reading at the time of service."
        },
        "serviceType": {
          "type": "string",
          "description": "The type of service performed (e.g., oil change, tire rotation)."
        },
        "mechanicDetails": {
          "type": "string",
          "description": "Details about the mechanic or service center."
        },
        "cost": {
          "type": "number",
          "description": "The cost of the maintenance service."
        },
        "billUrl": {
          "type": "string",
          "description": "URL of the uploaded bill for the service.",
          "format": "uri"
        }
      },
      "required": [
        "id",
        "vehicleId",
        "date",
        "odometerReading",
        "serviceType",
        "mechanicDetails",
        "cost",
        "billUrl"
      ]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the application, including their profile and emergency information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user (matches Firebase Auth UID)."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "name": {
          "type": "string",
          "description": "The user's display name."
        },
        "phone": {
          "type": "string",
          "description": "The user's phone number."
        },
        "address": {
          "type": "string",
          "description": "The user's mailing address."
        },
        "photoURL": {
          "type": "string",
          "description": "URL of the user's profile picture.",
          "format": "uri"
        },
        "fcmTokens": {
          "type": "array",
          "description": "Array of Firebase Cloud Messaging tokens for push notifications.",
          "items": {
            "type": "string"
          }
        },
        "emergencyContact": {
          "type": "object",
          "description": "The user's primary emergency contact.",
          "properties": {
            "name": {
              "type": "string"
            },
            "phone": {
              "type": "string"
            },
            "relation": {
              "type": "string"
            }
          }
        },
        "medicalInfo": {
          "type": "object",
          "description": "The user's critical medical information.",
          "properties": {
            "bloodType": {
              "type": "string"
            },
            "allergies": {
              "type": "string"
            },
            "conditions": {
              "type": "string"
            }
          }
        }
      },
      "required": [
        "id",
        "email",
        "name"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profile information, including emergency contacts and medical info. The document ID is the user's UID.",
          "params": [
            {
              "name": "userId",
              "description": "The Firebase Auth UID of the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/vehicles/{vehicleId}",
        "definition": {
          "entityName": "Vehicle",
          "schema": {
            "$ref": "#/backend/entities/Vehicle"
          },
          "description": "Stores vehicle information for a specific user.  Ownership is determined by the userId in the path.",
          "params": [
            {
              "name": "userId",
              "description": "The Firebase Auth UID of the user who owns the vehicle."
            },
            {
              "name": "vehicleId",
              "description": "The unique ID of the vehicle."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/vehicles/{vehicleId}/documents/{documentId}",
        "definition": {
          "entityName": "Document",
          "schema": {
            "$ref": "#/backend/entities/Document"
          },
          "description": "Stores documents related to a specific vehicle owned by a user. Ownership is determined by the userId and vehicleId in the path.",
          "params": [
            {
              "name": "userId",
              "description": "The Firebase Auth UID of the user who owns the vehicle."
            },
            {
              "name": "vehicleId",
              "description": "The unique ID of the vehicle to which the document belongs."
            },
            {
              "name": "documentId",
              "description": "The unique ID of the document."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/vehicles/{vehicleId}/maintenanceLogs/{maintenanceLogId}",
        "definition": {
          "entityName": "MaintenanceLog",
          "schema": {
            "$ref": "#/backend/entities/MaintenanceLog"
          },
          "description": "Stores maintenance logs for a specific vehicle owned by a user. Ownership is determined by the userId and vehicleId in the path.",
          "params": [
            {
              "name": "userId",
              "description": "The Firebase Auth UID of the user who owns the vehicle."
            },
            {
              "name": "vehicleId",
              "description": "The unique ID of the vehicle to which the maintenance log belongs."
            },
            {
              "name": "maintenanceLogId",
              "description": "The unique ID of the maintenance log."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to ensure authorization independence, clarity, and scalability. It leverages path-based ownership for user-specific data and denormalization to avoid complex security rules with `get()` calls.\n\n*   **/users/{userId}**: Stores user profiles, including nested objects for emergency and medical information for fast retrieval. Authorization is based on the `request.auth.uid` matching the `{userId}`.\n*   **/users/{userId}/vehicles/{vehicleId}**: Stores vehicle data owned by a specific user. The `userId` is embedded in the path, making ownership clear and enabling simple security rules.\n*   **/users/{userId}/vehicles/{vehicleId}/documents/{documentId}**: Stores documents related to a specific vehicle. The path includes both `userId` and `vehicleId`, ensuring that only the owner of the vehicle can access these documents.\n*   **/users/{userId}/vehicles/{vehicleId}/maintenanceLogs/{maintenanceLogId}**: Stores maintenance logs for a specific vehicle, following the same ownership pattern as documents.\n\n**Authorization Independence (CRITICAL):** The structure avoids `get()` calls in security rules by embedding ownership information within the document paths. This enables atomic operations and simplifies security rule logic.  For example, to create a new document for a vehicle, the rule only needs to check that `request.auth.uid == userId` from the path.  It does not need to `get()` the vehicle document to verify ownership.\n\nThis updated structure removes the separate `emergencyContacts` collection and nests that information within the `user` document itself. This is a deliberate denormalization strategy to ensure that in an emergency scenario, all of a user's critical information can be retrieved with a single document read, which is faster and more efficient."
  }
}
